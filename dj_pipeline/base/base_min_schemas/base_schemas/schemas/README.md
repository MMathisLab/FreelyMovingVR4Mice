### Mathis lab base tables for all datajoint pipelines ###

The `base_schema` folder is a pip installable package that contains all schemas (`mice` and `exp`) to share across different pipelines.

Installation:
```
pip install base_schemas
```
