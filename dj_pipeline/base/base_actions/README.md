### Mathis lab base tables for all datajoint pipelines ###

The `base_actions` folder is a pip installable package that contains login files and datajoint config to share across different pipelines.

Installation:
```
pip install base_actions
```
