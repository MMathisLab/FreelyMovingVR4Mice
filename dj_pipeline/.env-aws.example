# AWS / remote DB credentials for cron_script_aws.sh (copy to .env-aws on the server).
# Used inside the client container before: python cron_scenario.py --aws

DJ_HOST=your-aws-db-hostname-or-ip:3306
DJ_USER=your-aws-db-user
DJ_PWD=your-aws-db-password
DJ_LAB=mathis-lab
GUI=True
EMAIL=False
