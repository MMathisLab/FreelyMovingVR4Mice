class_name: DetWithVelThrTask
description: |
  Detection task with velocity threshold to initiate the trials.

# Only the parameters that differ from configs/common.yaml.
params:
  session_label:
  - ar_detection_velthr
  epoch_labels:
  - single_teardrop
  slit_size:
  - 4.0
  - 4.0
  - 1
  distractor: 0.0
